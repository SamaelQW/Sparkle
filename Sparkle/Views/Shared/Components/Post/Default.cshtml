@model  IEnumerable<Post>

@{
    foreach (var post in Model)
    {
        <div class="post">
            <h3 class="post__title">@post.Title</h3>
            <p class="post__body">@post.Body</p>
            <p class="post__created-time">@post.CreatedDate.ToString("dd/MM/yyyy HH:mm")</p>
        </div>
    }
    
}
